import React/*, { Component } */ from 'react';
import { /* All the elements you need*/ } from 'react-native';


// Create component
class /*Name */ extends Component {
//Class initial state, in this case it's an empty array called Albums
  state = { albums: [] };


  //Default function needed for all class based components, renders JSX
  render() {

  }
}

const /*NAME*/ = () => {

    //Return som JSX
    return (

    );
};

// Make the component publicly available
export default /*NAME*/;
